import { DateFocus } from './date-focus';

describe('DateFocus', () => {
  it('should create an instance', () => {
    const directive = new DateFocus();
    expect(directive).toBeTruthy();
  });
});
