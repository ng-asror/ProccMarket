import { AmDateFormatPipe } from './am-date-format-pipe';

describe('AmDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AmDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
